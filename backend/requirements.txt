fastapi>=0.115
uvicorn[standard]>=0.30
paper-qa>=5.0
# paper-qa 2026.8.12's agent orchestration calls LiteLLMModel.get_router(),
# which fhlmi removed in 0.47.0+. Pin to the last version that still has it
# until paper-qa releases a fix. See backend/paperqa_engine.py.
fhlmi==0.46.2
sentence-transformers>=3.0
arxiv>=2.1
httpx>=0.27
python-dotenv>=1.0
anthropic>=0.40

# Dev/test only — regression + eval scripts under backend/tests/ make real
# Anthropic API calls (they test actual model behavior, not mocks — see each
# file's module docstring), so running the full suite costs a few cents and
# needs ANTHROPIC_API_KEY set; tests skip themselves if it's missing.
pytest>=8.0
pytest-asyncio>=0.24
